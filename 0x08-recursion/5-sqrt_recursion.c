#include "main.h"

int find_sqrt(int num, int root);
int _sqrt_recursion(int n);

/**
 * find_sqrt - Finds the natural square inputted
 * @num: The number to find the square root
 * @root: The root to be tested
 *
 * Return: If the number has a natural square root
 * If the number does not have a natural square
 */
int find_sqrt(int num, int root)
{
	if ((root * root) == num)
		return (root);

	if 
	return (find_sqrt(num, root + 1));
